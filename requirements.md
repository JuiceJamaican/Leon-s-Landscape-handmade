## Packages
framer-motion | Smooth page transitions and hover effects
react-hook-form | robust form handling for admin panel
@hookform/resolvers | zod integration for form validation

## Notes
Admin route /admin requires authentication via Replit Auth (/api/login).
YouTube links are stored as an array of strings in the site_content table.
Images are stored as URL strings.
